#ifndef _silver_
#define _silver_

#define exports_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  token,  version) \
    M(X, Y, i, prop,     public,  path,   module_file) \
    M(X, Y, i, prop,     public,  path,   project_path) \
    M(X, Y, i, prop,     public,  path,   module_path) \
    M(X, Y, i, prop,     public,  map,    env_vars)
declare_class(exports)

// Clint M. Strandhagen -- the motivation for orbiter, which we design in silver
#define silver_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  bool,   watch) \
    M(X, Y, i, prop,     public,  bool,   clean) \
    M(X, Y, i, prop,     intern,  silver, is_external) \
    M(X, Y, i, prop,     public,  bool,   is_project) \
    M(X, Y, i, prop,     intern,  bool,   clipping) \
    M(X, Y, i, prop,     intern,  bool,   left_hand) \
    M(X, Y, i, prop,     public,  OPType, assign_type) \
    M(X, Y, i, prop,     intern,  bool,   in_args) \
    M(X, Y, i, prop,     public,  bool,   version) \
    M(X, Y, i, prop,     iobject, path,   product) \
    M(X, Y, i, prop,     public,  string, breakpoint) \
    M(X, Y, i, prop,     public,  i32,    etype_level) \
    M(X, Y, i, prop,     public,  i32,    parens_depth) \
    M(X, Y, i, prop,     public,  string, git_service) \
    M(X, Y, i, prop,     public,  string, git_owner) \
    M(X, Y, i, prop,     public,  string, git_project) \
    M(X, Y, i, prop,     public,  string, platform) \
    M(X, Y, i, prop,     intern,  string, docker) \
    M(X, Y, i, prop,     public,  string, source_raw) \
    M(X, Y, i, prop,     public,  path,   src_loc) \
    M(X, Y, i, prop,     intern,  bool,   processed_imports) \
    M(X, Y, i, prop,     intern,  bool,   has_module_func) \
    M(X, Y, i, prop,     intern,  bool,   has_module_type) \
    M(X, Y, i, prop,     iobject, array,  implements) \
    M(X, Y, i, prop,     public,  enode,  last_sub_return) \
    M(X, Y, i, prop,     public,  enode,  last_break) \
    M(X, Y, i, prop,     public,  enode,  last_continue) \
    M(X, Y, i, prop,     public,  token,  exported_version) \
    M(X, Y, i, prop,     public,  map,    exports) \
    M(X, Y, i, prop,     public,  map,    defs) \
    M(X, Y, i, prop,     public,  map,    defs_expect) \
    M(X, Y, i, prop,     public,  map,    defs_used) \
    M(X, Y, i, prop,     public,  array,  run) \
    M(X, Y, i, prop,     public,  string, debug_type) \
    M(X, Y, i, prop,     public,  string, debugmember) \
    M(X, Y, i, prop,     public,  string, defs_hash) \
    M(X, Y, i, prop,     public,  path,   build_dir) \
    M(X, Y, i, prop,     public,  path,   product_link) \
    M(X, Y, i, prop,     public,  array,  artifacts) \
    M(X, Y, i, prop,     public,  path,   artifacts_path) \
    M(X, Y, i, prop,     public,  array,  resources) \
    M(X, Y, i, prop,     public,  bool,   expect_state) \
    M(X, Y, i, prop,     public,  bool,   error) \
    M(X, Y, i, prop,     public,  map,    codegens) \
    M(X, Y, i, prop,     public,  bool,   strict) \
    M(X, Y, i, prop,     iobject, map,    import_cache) \
    M(X, Y, i, prop,     intern,  array,  pending_aliases) \
    M(X, Y, i, method,   public,  none,   write_header) \
    M(X, Y, i, method,   public,  enode,  parse_member_expr, enode, bool) \
    M(X, Y, i, method,   public,  none,   incremental_resolve) \
    M(X, Y, i, method,   public,  token,  next) \
    M(X, Y, i, method,   public,  token,  prev) \
    M(X, Y, i, method,   public,  token,  navigate, i32) \
    M(X, Y, i, method,   public,  token,  consume) \
    M(X, Y, i, method,   public,  token,  peek) \
    M(X, Y, i, method,   public,  token,  element, num) \
    M(X, Y, i, method,   public,  num,    current_line) \
    M(X, Y, i, method,   public,  bool,   next_is_alpha) \
    M(X, Y, i, method,   public,  bool,   next_indent) \
    M(X, Y, i, method,   public,  enode,  read_enode, etype, bool, bool) \
    M(X, Y, i, method,   public,  enode,  parse_member, ARef, Au_t, etype, bool) \
    M(X, Y, i, method,   public,  enode,  parse_ternary, enode, etype, bool) \
    M(X, Y, i, method,   public,  enode,  parse_assignment, enode, OPType, bool) \
    M(X, Y, i, method,   public,  token,  read_if, symbol) \
    M(X, Y, i, method,   public,  Au,     read_literal, Au_t) \
    M(X, Y, i, method,   public,  string, read_string) \
    M(X, Y, i, method,   public,  bool,   is_cmode) \
    M(X, Y, i, method,   public,  string, read_alpha) \
    M(X, Y, i, method,   public,  string, read_alpha_any) \
    M(X, Y, i, method,   public,  string, peek_alpha) \
    M(X, Y, i, method,   public,  bool,   next_is_neighbor) \
    M(X, Y, i, method,   public,  etype,  read_def, interface) \
    M(X, Y, i, method,   public,  string, peek_def) \
    M(X, Y, i, method,   public,  string, peek_keyword) \
    M(X, Y, i, method,   public,  string, read_keyword) \
    M(X, Y, i, method,   public,  Au,     read_bool) \
    M(X, Y, i, method,   public,  Au,     read_numeric) \
    M(X, Y, i, method,   public,  array,  parse_const, array) \
    M(X, Y, i, method,   public,  OPType, read_operator, ARef) \
    M(X, Y, i, method,   public,  string, location) \
    M(X, Y, i, method,   public,  none,   parse) \
    M(X, Y, i, method,   public,  none,   build) \
    M(X, Y, i, ctr,      public,  path) \
    M(X, Y, i, override, method,  build_user_initializer) \
    M(X, Y, i, override, method,  emit_override_init) \
    M(X, Y, i, override, method,  build_attrib_value) \
    M(X, Y, i, override, method,  init)
declare_class_4(silver, aether, emodule, etype)

#define chatgpt_module_ silver
#define chatgpt_schema(X, Y, ...) \
    M(X, Y, i, override, method,  generate_fn) \
    M(X, Y, i, prop,     public,  string, model)
declare_class_2(chatgpt, codegen)

#define claude_module_ silver
#define claude_schema(X, Y, ...) \
    M(X, Y, i, override, method,  generate_fn) \
    M(X, Y, i, prop,     public,  string, model)
declare_class_2(claude, codegen)

#define gemini_module_ silver
#define gemini_schema(X, Y, ...) \
    M(X, Y, i, override, method,  generate_fn) \
    M(X, Y, i, prop,     public,  string, model)
declare_class_2(gemini, codegen)

#endif
