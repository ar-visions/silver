#ifndef _AETHER2_
#define _AETHER2_
#define aclang_cc_module_ aether
#define aether_module_ aether
#define catcher_module_ aether
#define code_module_ aether
#define codegen_module_ aether
#define edecl_module_ aether
#define efunc_module_ aether
#define emeta_module_ aether
#define emodule_module_ aether
#define enamespace_module_ aether
#define enode_module_ aether
#define etype_module_ aether
#define evar_module_ aether
#define import_module_ aether
#define macro_module_ aether
#define statements_module_ aether
#define static_array_module_ aether

// one LLVM context per codegen core; every context-bound handle is a
// slot on the single etype/enode instance, indexed by core
#define AU_CORES 8

forward(etype)
forward(aether)
forward(enode)
forward(code)
forward(catcher)
forward(codegen)

#define u(E, _au) ((E)(instance_of((Au)get(a->registry, (Au)(_au)), typeid(E))))
AU_EXPORT etype rlookup(aether a, string name);

#define efind(E, au, N) ({ \
    enum AU_MEMBER m = 0; \
    if      (strcmp(typeid(E)->ident, "efunc") == 0) m = AU_MEMBER_FUNC; \
    else if (strcmp(typeid(E)->ident, "evar")  == 0) m = AU_MEMBER_VAR; \
    Au_t f = find_member(au_arg((Au)au), #N, m, 0, false); \
    (E)u(E, f); \
})

#define etypeid(ID) u(etype, typeid(ID))

#define members(MDL, VAR) \
    for (int __i = 0; __i < (MDL)->members.count; __i++) \
        for (Au_t VAR = (Au_t)(MDL)->members.origin[__i]; VAR; VAR = NULL)

#define arg_list(MDL, VAR) \
    for (int __i = 0; __i < (MDL)->args.count; __i++) \
        for (Au_t VAR = (Au_t)(MDL)->args.origin[__i]; VAR; VAR = NULL)

#define arg_types(MDL, VAR) \
    for (int __i = 0; __i < (MDL)->args.count; __i++) \
        for (Au_t VAR = (Au_t)read_arg_type((Au_t)(MDL)->args.origin[__i]); VAR; VAR = NULL)

AU_EXPORT Au_t read_arg_type(Au_t stored_arg);

forward(enode)

#define etype_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  Au_t,            autype) \
    M(X, Y, i, array,    intern,  ARef,            AU_CORES, lltypes) \
    M(X, Y, i, array,    intern,  ARef,            AU_CORES, llscopes) \
    M(X, Y, i, array,    intern,  ARef,            AU_CORES, lldebugs) \
    M(X, Y, i, prop,     intern,  bool,            is_implemented) \
    M(X, Y, i, prop,     public,  bool,            is_explicit_ref) \
    M(X, Y, i, prop,     public,  Au,              meta_a) \
    M(X, Y, i, prop,     public,  Au,              meta_b) \
    M(X, Y, i, prop,     public,  array,           dims) \
    M(X, Y, i, prop,     public,  aether,          mod, meta, weak) \
    M(X, Y, i, prop,     public,  etype,           schema) \
    M(X, Y, i, prop,     public,  etype,           suffix) \
    M(X, Y, i, prop,     public,  enode,           eshape) \
    M(X, Y, i, prop,     public,  tokens,          body) \
    M(X, Y, i, prop,     public,  i32,             iteration) \
    M(X, Y, i, prop,     public,  bool,            is_any) \
    M(X, Y, i, prop,     public,  bool,            has_code) \
    M(X, Y, i, prop,     public,  bool,            remote_code) \
    M(X, Y, i, prop,     public,  bool,            is_schema) \
    M(X, Y, i, prop,     public,  bool,            is_elsewhere) \
    M(X, Y, i, prop,     public,  bool,            user_built) \
    M(X, Y, i, prop,     public,  bool,            parsing) \
    M(X, Y, i, prop,     public,  path,            imported_from) \
    M(X, Y, i, prop,     public,  codegen,         cgen) \
    M(X, Y, i, prop,     public,  enode,           type_id) \
    M(X, Y, i, method,   public,  enode,           access, string) \
    M(X, Y, i, method,   public,  none,            implement, bool) \
    M(X, Y, i, method,   public,  array,           class_list) \
    M(X, Y, i, method,   public,  etype,           resolve) \
    M(X, Y, i, guard,    public,  etype,           canonical, etype) \
    M(X, Y, i, override, method,  copy) \
    M(X, Y, i, override, method,  init) \
    M(X, Y, i, override, cast,    Au_t) \
    M(X, Y, i, override, cast,    string)
declare_class(etype)

#define edecl_schema(X, Y, ...)
declare_class_2(edecl, etype)

#define emodule_schema(X, Y, ...)
declare_class_2(emodule, etype)

#define statements_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  i32,             probe_id) \
    M(X, Y, i, prop,     public,  i32,             probe_line)
declare_class_2(statements, etype)

forward(efunc)
forward(evar)

#define aether_schema(A, U, ...) \
    M(A,U,i, prop,     public,  bool,                     component) \
    M(A,U,i, prop,     public,  string,                   name) \
    M(A,U,i, prop,     def,     path,                     module) \
    M(A,U,i, prop,     public,  path,                     module_file) \
    M(A,U,i, prop,     public,  path,                     root_path) \
    M(A,U,i, prop,     public,  path,                     module_path) \
    M(A,U,i, prop,     public,  path,                     project_path) \
    M(A,U,i, prop,     public,  bool,                     building_initializer) \
    M(A,U,i, prop,     public,  bool,                     init_props_retain_skip) \
    M(A,U,i, prop,     public,  bool,                     direct) \
    M(A,U,i, prop,     public,  bool,                     import_c) \
    M(A,U,i, prop,     public,  bool,                     release) \
    M(A,U,i, prop,     public,  bool,                     strip_expect) \
    M(A,U,i, prop,     public,  bool,                     debug) \
    M(A,U,i, prop,     public,  bool,                     asan) \
    M(A,U,i, prop,     public,  bool,                     coverage) \
    M(A,U,i, prop,     public,  bool,                     timing) \
    M(A,U,i, prop,     public,  i32,                      next_func_id) \
    M(A,U,i, prop,     public,  i32,                      next_probe_id) \
    M(A,U,i, prop,     public,  path,                     current_import) \
    M(A,U,i, prop,     public,  i32,                      expr_level) \
    M(A,U,i, prop,     public,  Au_t,                     import_module) \
    M(A,U,i, prop,     intern,  array,                    import_inits) \
    M(A,U,i, prop,     public,  array,                    import_objects) \
    M(A,U,i, prop,     public,  Au_t,                     model_scope) \
    M(A,U,i, prop,     public,  Au_t,                     au_module) \
    M(A,U,i, prop,     public,  map,                      libs) \
    M(A,U,i, prop,     public,  map,                      frameworks) \
    M(A,U,i, prop,     public,  map,                      user_type_ids) \
    M(A,U,i, prop,     public,  array,                    lexical) \
    M(A,U,i, prop,     public,  bool,                     is_Au_import) \
    M(A,U,i, prop,     public,  bool,                     in_ref) \
    M(A,U,i, prop,     public,  array,                    sys_inc_paths) \
    M(A,U,i, prop,     public,  array,                    sys_exc_paths) \
    M(A,U,i, prop,     public,  array,                    include_paths) \
    M(A,U,i, prop,     public,  array,                    framework_paths) \
    M(A,U,i, prop,     public,  array,                    lib_paths) \
    M(A,U,i, prop,     public,  path,                     isysroot) \
    M(A,U,i, prop,     public,  path,                     resource_dir) \
    M(A,U,i, prop,     public,  path,                     isystem) \
    M(A,U,i, prop,     public,  bool,                     verbose) \
    M(A,U,i, prop,     public,  path,                     install) \
    M(A,U,i, prop,     public,  path,                     build_dir) \
    M(A,U,i, prop,     public,  path,                     target_sysroot) \
    M(A,U,i, prop,     public,  bool,                     is_library) \
    M(A,U,i, prop,     public,  bool,                     has_coverage) \
    M(A,U,i, prop,     public,  bool,                     is_live) \
    M(A,U,i, prop,     public,  bool,                     cmode) \
    M(A,U,i, prop,     public,  bool,                     no_build) \
    M(A,U,i, prop,     public,  bool,                     type_given) \
    M(A,U,i, prop,     public,  bool,                     skip_context_resolve) \
    M(A,U,i, prop,     public,  bool,                     deferred_hit) \
    M(A,U,i, prop,     public,  bool,                     no_const) \
    M(A,U,i, prop,     public,  bool,                     is_const_op) \
    M(A,U,i, prop,     public,  bool,                     iterator_guard) \
    M(A,U,i, prop,     public,  bool,                     debug_typeid) \
    M(A,U,i, prop,     public,  bool,                     is_delegate) \
    M(A,U,i, prop,     public,  array,                    setter_key_tokens) \
    M(A,U,i, prop,     public,  Au_t,                     setter_fn) \
    M(A,U,i, prop,     public,  i32,                      stack_test) \
    M(A,U,i, prop,     public,  enode,                    last_return) \
    M(A,U,i, prop,     public,  efunc,                    fn_init) \
    M(A,U,i, prop,     public,  callback,                 prepare_record) \
    M(A,U,i, prop,     public,  callback_extra,           emit_overrides) \
    M(A,U,i, prop,     public,  callback_extra,           parse_f) \
    M(A,U,i, prop,     public,  callback,                 parse_enode) \
    M(A,U,i, prop,     public,  callback,                 parse_expr) \
    M(A,U,i, prop,     public,  callback_extra,           read_etype) \
    M(A,U,i, prop,     intern,  LLVMTypeRef,              func_timing_lltype,      as, ARef) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             func_timings_global,     as, ARef) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             func_names_global,       as, ARef) \
    M(A,U,i, prop,     intern,  LLVMTypeRef,              clock_gettime_type,      as, ARef) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             clock_gettime_fn,        as, ARef) \
    M(A,U,i, prop,     intern,  LLVMTypeRef,              coverage_probe_lltype,   as, ARef) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             coverage_probes_global,  as, ARef) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             coverage_seq_global,     as, ARef) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             coverage_seq_local,      as, ARef) \
    M(A,U,i, prop,     public,  bool,                     listen_active) \
    M(A,U,i, prop,     public,  bool,                     listen_values) \
    M(A,U,i, prop,     public,  bool,                     listen_emitting) \
    M(A,U,i, prop,     intern,  LLVMValueRef,             listen_gate,       as, ARef) \
    M(A,U,i, prop,     public,  string,                   watch_class) \
    M(A,U,i, prop,     public,  string,                   watch_member) \
    M(A,U,i, prop,     intern,  token,                    listen_last_token, as, ARef) \
    M(A,U,i, prop,     intern,  LLVMExecutionEngineRef,   jit,                     as, ARef) \
    M(A,U,i, prop,     intern,  LLVMBasicBlockRef,        current_if_block,        as, ARef) \
    M(A,U,i, prop,     intern,  LLVMModuleRef,            module_ref,              as, ARef) \
    M(A,U,i, prop,     intern,  LLVMContextRef,           module_ctx,              as, ARef) \
    M(A,U,i, prop,     public,  i32,                      ncores) \
    M(A,U,i, prop,     intern,  array,                    ctxs) \
    M(A,U,i, prop,     intern,  array,                    mods) \
    M(A,U,i, prop,     intern,  array,                    dbgs) \
    M(A,U,i, prop,     intern,  array,                    files) \
    M(A,U,i, prop,     intern,  array,                    cus) \
    M(A,U,i, prop,     intern,  LLVMBuilderRef,           builder,                 as, ARef) \
    M(A,U,i, prop,     intern,  LLVMMetadataRef,          file,                    as, ARef) \
    M(A,U,i, prop,     intern,  LLVMMetadataRef,          current_file,            as, ARef) \
    M(A,U,i, prop,     intern,  LLVMMetadataRef,          compile_unit,            as, ARef) \
    M(A,U,i, prop,     intern,  LLVMDIBuilderRef,         dbg_builder,             as, ARef) \
    M(A,U,i, prop,     intern,  LLVMTargetRef,            target_ref,              as, ARef) \
    M(A,U,i, prop,     intern,  LLVMTargetDataRef,        target_data,             as, ARef) \
    M(A,U,i, array,    intern,  ARef,            AU_CORES, target_datas) \
    M(A,U,i, prop,     intern,  LLVMTargetMachineRef,     target_machine,          as, ARef) \
    M(A,U,i, array,    intern,  ARef,            AU_CORES, target_machines) \
    M(A,U,i, prop,     intern,  cstr,                     target_triple) \
    M(A,U,i, prop,     public,  token,                    statement_origin) \
    M(A,U,i, prop,     public,  store,                    registry) \
    M(A,U,i, prop,     public,  i32,                      core) \
    M(A,U,i, prop,     public,  aether,                   root, meta, weak) \
    M(A,U,i, prop,     public,  tokens,                   tokens) \
    M(A,U,i, prop,     public,  num,                      cursor) \
    M(A,U,i, prop,     public,  array,                    stack) \
    M(A,U,i, prop,     public,  Au_t,                     top) \
    M(A,U,i, prop,     public,  string,                   dbg_init) \
    M(A,U,i, prop,     public,  string,                   dbg_implement) \
    M(A,U,i, prop,     public,  string,                   break_id) \
    M(A,U,i, prop,     public,  map,                      init_props) \
    M(A,U,i, prop,     public,  map,                      vec_types) \
    M(A,U,i, prop,     public,  handle,                   init_fbits) \
    M(A,U,i, prop,     public,  cstr,                     bind_name) \
    M(A,U,i, prop,     public,  Au_t,                     bind_holder) \
    M(A,U,i, prop,     public,  Au_t,                     bind_au) \
    M(A,U,i, prop,     public,  array,                    imports) \
    M(A,U,i, vargs,    public,  none,                     eprint, symbol) \
    M(A,U,i, method,   public,  none,                     e_vector_init, etype, enode, array) \
    M(A,U,i, method,   public,  none,                     e_setenv, symbol, symbol) \
    M(A,U,i, method,   public,  none,                     create_type_members, Au_t) \
    M(A,U,i, method,   public,  none,                     build_module_initializer, enode) \
    M(A,U,i, method,   public,  efunc,                    module_initializer) \
    M(A,U,i, method,   public,  none,                     import_Au, string, Au) \
    M(A,U,i, method,   public,  efunc,                    function, etype, string, etype, array, u8, u32, u8) \
    M(A,U,i, method,   public,  etype,                    record, etype, etype, string, u32) \
    M(A,U,i, method,   public,  none,                     reinit_startup) \
    M(A,U,i, method,   public,  none,                     unload_libs) \
    M(A,U,i, method,   public,  enode,                    e_alloc, etype, bool) \
    M(A,U,i, method,   public,  bool,                     e_fn_return, Au) \
    M(A,U,i, method,   public,  none,                     e_cond_return, enode, Au) \
    M(A,U,i, method,   public,  enode,                    e_null, etype) \
    M(A,U,i, method,   public,  enode,                    e_default_value, etype) \
    M(A,U,i, method,   public,  enode,                    e_builder, subprocedure) \
    M(A,U,i, method,   public,  none,                     build_user_initializer, etype) \
    M(A,U,i, method,   public,  none,                     emit_override_init, enode, Au_t) \
    M(A,U,i, method,   public,  enode,                    build_attrib_value, evar) \
    M(A,U,i, method,   public,  enode,                    e_subroutine, etype, array, subprocedure) \
    M(A,U,i, method,   public,  none,                     e_cmp_code, enode, comparison, enode, code, code) \
    M(A,U,i, method,   public,  enode,                    e_cmp, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_cmp_op, OPType, enode, enode) \
    M(A,U,i, method,   public,  none,                     e_branch, code) \
    M(A,U,i, method,   public,  enode,                    e_expect, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_expect_begin, enode) \
    M(A,U,i, method,   public,  enode,                    e_expect_end, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_fault, enode) \
    M(A,U,i, method,   public,  enode,                    e_log, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_break, catcher) \
    M(A,U,i, method,   public,  enode,                    e_continue, catcher) \
    M(A,U,i, method,   public,  none,                     e_memcpy, enode, enode, Au_t) \
    M(A,U,i, method,   public,  enode,                    e_operand, Au, etype) \
    M(A,U,i, method,   public,  enode,                    e_short_circuit, OPType, enode) \
    M(A,U,i, method,   public,  enode,                    e_op, OPType, string, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_load, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_typeid, etype) \
    M(A,U,i, method,   public,  enode,                    e_meta_ids, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_eval, string) \
    M(A,U,i, method,   public,  enode,                    e_interpolate, string) \
    M(A,U,i, method,   public,  enode,                    e_const_array, etype, array) \
    M(A,U,i, method,   public,  enode,                    e_offset, enode, Au, bool) \
    M(A,U,i, method,   public,  enode,                    e_switch, enode, map, array, subprocedure, subprocedure) \
    M(A,U,i, method,   public,  enode,                    e_native_switch, enode, map, array, subprocedure, subprocedure) \
    M(A,U,i, method,   public,  enode,                    e_for, array, array, array, array, subprocedure, subprocedure, subprocedure, subprocedure, bool, enode, evar, evar, bool) \
    M(A,U,i, method,   public,  enode,                    e_if_else, array, array, subprocedure, subprocedure) \
    M(A,U,i, method,   public,  enode,                    e_init, enode, map, efunc, enode) \
    M(A,U,i, method,   public,  enode,                    e_ternary, enode, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_ternary_deferred, enode, array, array, subprocedure) \
    M(A,U,i, method,   public,  enode,                    e_coalesce_deferred, enode, array, subprocedure) \
    M(A,U,i, method,   public,  enode,                    e_primitive_convert, enode, etype) \
    M(A,U,i, method,   public,  enode,                    e_direct_cast, enode, etype) \
    M(A,U,i, method,   public,  enode,                    e_noop, etype) \
    M(A,U,i, method,   public,  enode,                    e_inherited_access, enode, Au_t) \
    M(A,U,i, method,   public,  enode,                    e_addr_of, enode, etype) \
    M(A,U,i, method,   public,  enode,                    e_bitwise_not, enode) \
    M(A,U,i, method,   public,  enode,                    e_not, enode) \
    M(A,U,i, method,   public,  enode,                    e_neg, enode) \
    M(A,U,i, method,   public,  enode,                    e_max, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_min, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_abs, enode) \
    M(A,U,i, method,   public,  enode,                    e_clamp, enode, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_math, i32, enode) \
    M(A,U,i, method,   public,  enode,                    e_math2, i32, enode, enode) \
    M(A,U,i, method,   public,  enode,                    e_stack_array, etype, i32) \
    M(A,U,i, method,   public,  enode,                    e_stack_array_dynamic, etype, enode) \
    M(A,U,i, method,   public,  none,                     e_inc, enode, num) \
    M(A,U,i, method,   public,  none,                     e_print_node, enode) \
    M(A,U,i, method,   public,  enode,                    e_or, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_xor, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_and, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_add, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_sub, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_mul, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_div, Au, Au) \
    M(A,U,i, method,   public,  enode,                    e_create, etype, Au, bool) \
    M(A,U,i, method,   public,  enode,                    e_vector, etype, enode) \
    M(A,U,i, method,   public,  enode,                    e_vec_new, etype) \
    M(A,U,i, method,   public,  etype,                    etype_vec, Au_t) \
    M(A,U,i, method,   public,  enode,                    e_vec_method, enode, symbol, array) \
    M(A,U,i, method,   public,  enode,                    e_zero, enode) \
    M(A,U,i, method,   public,  enode,                    e_assign, enode, Au, OPType) \
    M(A,U,i, method,   public,  none,                     e_assign_if_null, enode, Au) \
    M(A,U,i, method,   public,  enode,                    e_fn_call, efunc, array, bool, bool) \
    M(A,U,i, method,   public,  enode,                    lambda_fcall, efunc, array) \
    M(A,U,i, method,   public,  enode,                    e_is, enode, Au) \
    M(A,U,i, method,   public,  enode,                    e_inherits, enode, Au) \
    M(A,U,i, method,   public,  none,                     llflag, symbol, i32) \
    M(A,U,i, method,   public,  bool,                     emit, ARef, ARef) \
    M(A,U,i, method,   public,  bool,                     emit_object, path) \
    M(A,U,i, method,   public,  string,                   core_objects, path) \
    M(A,U,i, method,   public,  none,                     set_target, symbol) \
    M(A,U,i, method,   public,  etype,                    return_type) \
    M(A,U,i, method,   public,  statements,               context_code) \
    M(A,U,i, method,   public,  efunc,                    context_func) \
    M(A,U,i, method,   public,  etype,                    context_class) \
    M(A,U,i, method,   public,  etype,                    context_struct) \
    M(A,U,i, method,   public,  etype,                    context_record) \
    M(A,U,i, method,   public,  etype,                    context_model, Au_t) \
    M(A,U,i, method,   public,  none,                     push_current) \
    M(A,U,i, method,   public,  none,                     push_tokens, tokens, num) \
    M(A,U,i, method,   public,  none,                     pop_tokens, bool) \
    M(A,U,i, method,   public,  path,                     lookup_include, string) \
    M(A,U,i, method,   public,  path,                     include, Au, string) \
    M(A,U,i, method,   public,  none,                     import_includes) \
    M(A,U,i, method,   public,  Au_t,                     top_scope) \
    M(A,U,i, method,   public,  none,                     push_scope, Au, i32) \
    M(A,U,i, method,   public,  Au_t,                     pop_scope) \
    M(A,U,i, method,   public,  enode,                    e_asm, array, array, etype, string) \
    M(A,U,i, override, method,  init) \
    M(A,U,i, override, method,  dealloc)
declare_class_3(aether, emodule, etype);

#define tokens_data_module_ aether
#define tokens_data_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  array,           tokens_list) \
    M(X, Y, i, prop,     public,  num,             cursor) \
    M(X, Y, i, prop,     public,  token,           saved_origin)
declare_class(tokens_data)

#define enamespace_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  token,           alias)
declare_class_2(enamespace, etype)

#define catcher_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  map,             team) \
    M(X, Y, i, prop,     public,  enode,           phi) \
    M(X, Y, i, prop,     public,  etype,           rtype) \
    M(X, Y, i, prop,     public,  bool,            last_return) \
    M(X, Y, i, prop,     opaque,  LLVMBasicBlockRef, block) \
    M(X, Y, i, prop,     opaque,  LLVMBasicBlockRef, continue_block)
declare_class_2(catcher, etype)

#define code_schema(X, Y, ...) \
    M(X, Y, i, prop,     intern,  LLVMBasicBlockRef, block, as, ARef) \
    M(X, Y, i, prop,     public,  aether,          mod, meta, weak) \
    M(X, Y, i, prop,     public,  symbol,          label) \
    M(X, Y, i, method,   public,  none,            seek_end) \
    M(X, Y, i, override, method,  init)
declare_class(code)

forward(evar)

#define enode_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  enode,           target) \
    M(X, Y, i, prop,     public,  bool,            is_super) \
    M(X, Y, i, prop,     public,  bool,            is_alloc) \
    M(X, Y, i, prop,     public,  bool,            is_assign) \
    M(X, Y, i, prop,     public,  bool,            avoid_ftable) \
    M(X, Y, i, prop,     public,  string,          symbol_name) \
    M(X, Y, i, prop,     public,  enode,           published_type) \
    M(X, Y, i, prop,     public,  Au,              literal) \
    M(X, Y, i, prop,     public,  u8,              arg_index) \
    M(X, Y, i, prop,     public,  bool,            used) \
    M(X, Y, i, prop,     public,  tokens,          initializer) \
    M(X, Y, i, prop,     public,  bool,            loaded) \
    M(X, Y, i, prop,     public,  bool,            is_elem_slot) \
    M(X, Y, i, prop,     public,  bool,            from_call) \
    M(X, Y, i, prop,     public,  string,          debug_id) \
    M(X, Y, i, array,    intern,  ARef,              AU_CORES, last_dbgs) \
    M(X, Y, i, array,    intern,  ARef,              AU_CORES, values) \
    M(X, Y, i, prop,     public,  enode,           context_node, as, ARef) \
    M(X, Y, i, array,    intern,  ARef,              AU_CORES, entries) \
    M(X, Y, i, method,   public,  Au,              literal_value, Au_t) \
    M(X, Y, i, method,   public,  enode,           retain) \
    M(X, Y, i, method,   public,  enode,           emit_release) \
    M(X, Y, i, method,   public,  none,            inspect) \
    M(X, Y, i, method,   public,  enode,           deref) \
    M(X, Y, i, override, cast,    Au_t) \
    M(X, Y, i, override, method,  init) \
    M(X, Y, i, override, method,  dealloc)
declare_class_2(enode, etype)

#define evar_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  bool,            is_local) \
    M(X, Y, i, prop,     public,  map,             placeholder)
declare_class_3(evar, enode, etype)

#define efunc_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  efunc,           remote_func) \
    M(X, Y, i, prop,     public,  array,           inline_return) \
    M(X, Y, i, prop,     public,  array,           const_tokens) \
    M(X, Y, i, prop,     intern,  i32,             timing_func_id,     as, i32) \
    M(X, Y, i, prop,     intern,  LLVMValueRef,    timing_start_value, as, ARef) \
    M(X, Y, i, prop,     public,  enode,           fptr) \
    M(X, Y, i, prop,     public,  token,           origin_token) \
    M(X, Y, i, prop,     public,  path,            source_file) \
    M(X, Y, i, prop,     public,  map,             placeholder)
declare_class_3(efunc, enode, etype)

#define emeta_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  i32,             meta_index)
declare_class_2(emeta, etype)

#define aclang_cc_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  aether,          mod, meta, weak) \
    M(X, Y, i, prop,     public,  handle,          compiler) \
    M(X, Y, i, prop,     public,  handle,          PP) \
    M(X, Y, i, prop,     public,  handle,          ctx) \
    M(X, Y, i, prop,     public,  handle,          consumer) \
    M(X, Y, i, prop,     public,  handle,          S) \
    M(X, Y, i, prop,     public,  handle,          P) \
    M(X, Y, i, prop,     public,  handle,          module)
declare_class(aclang_cc)

#define macro_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  array,           def) \
    M(X, Y, i, prop,     public,  array,           params) \
    M(X, Y, i, prop,     public,  bool,            va_args) \
    M(X, Y, i, prop,     public,  bool,            expanding) \
    M(X, Y, i, method,   public,  array,           expand, array)
declare_class_2(macro, etype)

#define codegen_schema(X, Y, ...) \
    M(X, Y, i, method,   public,  array,           generate_fn, efunc, array) \
    M(X, Y, i, prop,     public,  aether,          mod) \
    M(X, Y, i, prop,     public,  i32,             pad)
declare_class(codegen)

#define static_array_schema(X, Y, ...) \
    M(X, Y, i, prop,     intern,  bool,            placeholder)
declare_class_3(static_array, array, collective)

#define import_schema(X, Y, ...) \
    M(X, Y, i, prop,     public,  codegen, codegen) \
    M(X, Y, i, prop,     public,  string,  external_name) \
    M(X, Y, i, prop,     public,  path,    external_product) \
    M(X, Y, i, prop,     public,  path,    host_product) \
    M(X, Y, i, prop,     public,  array,   tokens) \
    M(X, Y, i, prop,     public,  path,    lib_path) \
    M(X, Y, i, prop,     public,  path,    module_source) \
    M(X, Y, i, prop,     public,  array,   include_paths) \
    M(X, Y, i, prop,     public,  map,     define_map) \
    M(X, Y, i, prop,     public,  bool,    is_cpp) \
    M(X, Y, i, prop,     public,  bool,    is_au_rt)
declare_class_3(import, enamespace, etype)

AU_EXPORT void alloc_origin_args(aether a, enode* out_src, Au* out_line, Au* out_seq);

#endif