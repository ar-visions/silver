add_executable (obj-test main.c)
if(MSVC)
set_source_files_properties (main.c PROPERTIES LANGUAGE CXX)
add_definitions(-DNOMINMAX -D_CRT_SECURE_NO_WARNINGS)
endif()
target_link_libraries (obj-test LINK_PUBLIC obj)
