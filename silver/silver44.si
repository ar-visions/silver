
class app [
    public int    arg: 0
    public string another
    intern string hidden: 'dragon'

    # called by run
    # no decorators inside of args.  no 'ref' in silver44
    # we should handle refs inside of object (A class)
    num something[ num m, real mag ] [ return i * 1 * mag ]

    # app entrance method; one could override init as well
    num run[] [
        num i: 2 + arg
        num a: 2
        num n: 1
        return n + something[ a + i, 2 ]
    ]
]