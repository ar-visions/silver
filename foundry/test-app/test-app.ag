
export 0.8.8
import <stdio.h>

import test3
    coolteen:'Clint'

import ai

app test-app
    a_param:int 2
    func init [] 
        t : tensor [scale: 1.0]
        puts 'this runs {t.scale} ? {'something {'inside another'}'}'


